zookeeper in Kafka and partition and offset related questons

Kafka Working flow Consumer and Producer

Kafka Streaming flow

Main features of Kafka

Consumer Group

Difference between Kafka and ActiveMQ.

To process a 500KB file which queue should be choose(Kafka or ActiveMQ).

If you have a producer in kafka. suppose you are producing A B C D. A & C belong to same category. B & D belong to same category. How will you ensure while reading same A will always read before C and B always before D.

How to integrate Kafka with your project ?

Kafka -> why kafka?, What makes kafka fast?, Working of Consumer group coordinator and how rebalance will happen?, Configuration used in the current project and daily load

Kafka use case in current project, configuration, daily load, asked few questions on failure point and error handlin

